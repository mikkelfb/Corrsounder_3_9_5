# auto-generated by grc.converter

id: corrsounder_error_correction_cc
label: Error Correction
category: '[Corrsounder]'

parameters:
-   id: ec_type
    label: Type
    dtype: enum
    default: through
    options: [through]
-   id: error_term_forward_transmission_tracking
    label: Error Term FTT
    dtype: complex_vector
    hide: ${ ('none' if ec_type == 'through' else 'all') }

inputs:
-   domain: stream
    dtype: complex
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: 1

templates:
    imports: import corrsounder
    make: corrsounder.error_correction_cc(${error_term_forward_transmission_tracking})

file_format: 1
