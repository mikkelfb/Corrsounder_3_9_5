# auto-generated by grc.converter

id: corrsounder_moving_average_vcc
label: Vector Moving Average
category: '[Corrsounder]/Utils'

parameters:
-   id: vlen
    label: Vec Len
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: window
    label: Window
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

templates:
    imports: import corrsounder
    make: corrsounder.moving_average_vcc(${vlen}, ${window})

file_format: 1
