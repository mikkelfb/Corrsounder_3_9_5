# auto-generated by grc.converter

id: corrsounder_seq_gate_cc
label: Sequence Gate
category: '[Corrsounder]'

parameters:
-   id: sequence_length
    label: Sequence Length
    dtype: int
    default: '255'
-   id: sample_rate
    label: Sample Rate
    dtype: float
    default: samp_rate
-   id: detect_overflows
    label: Buffer Overflow Source
    dtype: enum
    default: none
    options: [none, uhd]
    option_labels: [None, UHD]
-   id: external_trigger_tag
    label: Ext. Trigger Tag Name
    dtype: string

inputs:
-   domain: stream
    dtype: complex
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: 1
-   domain: message
    id: meta
    optional: true

templates:
    imports: import corrsounder
    make: corrsounder.sequence_gate_cc(${sequence_length}, ${sample_rate})

file_format: 1
