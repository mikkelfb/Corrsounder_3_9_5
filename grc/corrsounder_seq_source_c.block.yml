# auto-generated by grc.converter

id: corrsounder_seq_source_c
label: Sequence Source
category: '[Corrsounder]'

parameters:
-   id: seq_type
    label: Sequence Type
    dtype: enum
    default: fzc
    options: [fzc]
    option_labels: [FZC]
-   id: q
    label: FZC Parameter q
    dtype: int
    default: '7'
    hide: ${ ('none' if seq_type == 'fzc' else 'all') }
-   id: n
    label: Sequence Length
    dtype: int
    default: '1024'

outputs:
-   domain: stream
    dtype: complex
    vlen: 1

templates:
    imports: import corrsounder
    make: |-
        % if seq_type == 'fzc':
        corrsounder.fzc_source_c(n_fzc=${n}, q=${q}, repeat=True)
        % endif
    callbacks:
    - set_q(${q})
    - set_n_fzc(${n})

file_format: 1
